The following packages are used in the project:
usb_cam
turtlebot3

Build the package using:
colcon build

Source the setup file:
source install/setup.bash

Launch the package using:
ros2 launch camera_subscriber launch_file.py
